# Core scientific stack
numpy>=1.26
pandas>=2.2
scipy>=1.15
matplotlib>=3.10
pillow>=11.1

# Notebook / analysis workflow
jupyter
ipykernel

# Deep learning and graph neural networks
torch>=2.4
torch-geometric>=2.6

# Used by scripts in tools/
python-docx>=1.1

# If installing on a CUDA Linux server, install the matching PyTorch wheel first, for example:
# pip install torch --index-url https://download.pytorch.org/whl/cu124
